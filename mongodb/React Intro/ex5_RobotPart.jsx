// TODO: Create the RobotPart Component having two props: name and material

export default function RobotPart({ name, material }) {
  return (
    <p>
      {name} made of {material}
    </p>
  );
}
